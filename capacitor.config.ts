import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nicolaspuriy.ecotrack',
  appName: 'EcoTrack',
  webDir: 'dist/ecotrack/browser'
};

export default config;
